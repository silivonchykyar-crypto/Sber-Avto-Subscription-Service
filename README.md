1. Установить зависимости:
pip install -r requinrements.txt
2. Запустить API:
uvicorn app:app --reload
3. Открыть:
http://127.0.0.1:8000/docs

Модель (model.pkl) не загружен в репозиторий из-за большого размера (53МВ).
Её можно получить, запустив train_model.ipynb.

Исходные данные не включены в репозиторий из-за большого размера.
